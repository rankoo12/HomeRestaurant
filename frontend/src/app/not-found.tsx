import { PlaceholderPage } from '@/components/templates/placeholder-page';

/**
 * Global 404. Fleshed out with illustrations + quick links in Phase 8
 * (see docs/specs/phases/error-and-empty-states.md).
 */
export default function NotFound() {
  return <PlaceholderPage title="Page not found" route="404" />;
}
